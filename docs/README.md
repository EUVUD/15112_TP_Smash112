# Docs

This folder keeps non-runtime project material out of the repository root.

- `ai_resources/`: behavior tree and AI reference PDFs.
- `course_artifacts/`: local course submission/demo exports ignored by git.
